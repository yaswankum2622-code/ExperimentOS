experimentos:
  target: dev
  outputs:
    dev:
      type:     sqlite
      threads:  1
      database: "../../data/events.db"
      schema:   main
      schemas_and_paths:
        main: "../../data/events.db"
