name: experimentos
version: '1.0.0'
config-version: 2
profile: experimentos

model-paths:  ["models"]
test-paths:   ["tests"]
seed-paths:   ["seeds"]
macro-paths:  ["macros"]
target-path:  "target"
clean-targets: ["target", "dbt_packages"]

models:
  experimentos:
    staging:
      +materialized: view
    marts:
      +materialized: table
